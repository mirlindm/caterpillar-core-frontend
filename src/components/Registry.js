import React, { Component } from 'react';

class Registry extends Component {
    render () {
        return (
            <div className="text-white">
                Perform Runtime Registry related HTTP Requests
            </div>
        );
    }
}

export default Registry;